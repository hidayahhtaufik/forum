{
  "name": "@forum/example-forum-scout",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FORUM news-scout agent: polls FX/macro news feeds, asks an LLM whether the headline warrants a new prediction market, then auto-creates it via market-api. The only reason markets exist on FORUM that humans never typed.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "openai": "^4.73.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
